- hosts: localhost
  gather_facts: false
  vars:
    host: "34.209.118.204"
    username: ""
    password: ""
    vdom: "root"
    

  tasks:
  - name: gather basic system status facts
    fortios_facts:
      host:  "{{ host }}"
      username: "{{ username }}"
      password: "{{ password }}"
      vdom:  "{{ vdom }}"
      ssl_verify: "False"
      gather_subset:
        - fact: 'system_status_select'